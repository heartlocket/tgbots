# Core bot requirements
python-telegram-bot==20.6
python-dotenv==1.0.0
openai==0.27.2
requests==2.28.2
aiohttp==3.8.4

# For async operations
asyncio

# For logging & error handling
python-dateutil==2.8.2

# If you're using SQLite, no additional DB requirements needed

# Azure specific (if deploying to Azure)
azure-identity
azure-storage-blob
azure-monitor-opentelemetry

# Optional but recommended for Azure deployment
applicationinsights>=0.11.9